# 專案規則：Git 自動版本控制與 GitHub 推送

- **自動 Git Commit & Push (Auto Git Push)**：
  每次在專案中完成任何程式碼修改、功能新增或驗證後，必須自動執行 `git add .`、撰寫清楚的繁體中文 `git commit` 說明訊息，並自動 `git push` 至 GitHub 遠端倉庫（`origin main`），確保線上版本與遠端備份同步，無須等待使用者再次提示。
